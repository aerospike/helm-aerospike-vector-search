cluster: {}  # Missing required cluster-name
feature-key-file: "/etc/aerospike/features.conf"
aerospike:
  seeds:
    - aerospike-node-1:
        port: 3000
service:
  ports:
    "5000": {}
interconnect:
  ports:
    "5001": {} 